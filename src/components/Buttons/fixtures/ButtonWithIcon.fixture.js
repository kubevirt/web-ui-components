import { ButtonWithIcon } from '..';

export default {
  component: ButtonWithIcon,
  props: {
    onClick: () => {},
    iconType: 'pf',
    icon: 'virtual-machine',
    label: 'Button With Icon'
  }
};
